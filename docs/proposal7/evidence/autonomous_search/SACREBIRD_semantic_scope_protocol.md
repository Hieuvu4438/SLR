# TRAIN comment/codebook scope check

2026-09-16. Follow-up to the completed exact join, not a repetition of it.
For this pass only, read the existing comments from all307rows of the same pinned
TRAIN CSV; the prior join's prohibition on semantic comment reading applied to
that completed pass. Do not modify the prior certificate or its read-scope claim.
Verify source hash before reading. No TEST file, model, score or video access.

Read primary-paper methods/codebook material relevant to TRAIN separately from
the TEST back-translation experiment. Determine what the flags and comments
actually attest: gloss/text discrepancy, direct visual verification, contextual
ambiguity, or reviewer uncertainty. Do not create per-example semantic labels,
minimal-pair truth, alternative positives, error-free controls or training filters.
No statistical efficacy test, category expansion or score-based example selection.

If illustrative original pairs are needed, use the first substantive reviewer
comment in source order, not a model-selected/favorable example. Fetch original
TRAIN gloss/German and existing TRAIN English by exact ID only. This is a reading
aid, not an independent linguistic annotation or a reusable counterfactual label.
Record reading coverage, provenance and the resulting permissible decision.

The key gate is whether this resource supplies video-grounded support for a
distinct open mechanism. If not, retain it as descriptive annotation evidence,
do not launch a noisy-label/margin/lexical/soft-positive rescue or open TEST data.
