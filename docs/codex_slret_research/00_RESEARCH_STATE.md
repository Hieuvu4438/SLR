# Current research state

Updated 2026-09-23; current HEAD `de2f07ecac9ce65563316fde0860f22e4d585acf`.
Earlier cycles retain their recorded base commit; no commits were made by this cycle.
Current user constraint: **continue without human annotation** (2026-09-22).
The proposed DGS review branch is declined and stopped. Do not request a reviewer
again, construct a review packet, or substitute AI-generated semantic labels.
Existing annotation evidence retains its original limited scope; human review is
not a prerequisite imposed on the whole research program.

Cycle6 checked an [existing-annotation diagnostic lead](evidence/ASL_MTP_DIAGNOSTIC_SCOPE.md).
It does not supply PHOENIX/DGS relevance judgments, and a downloadable release
was not located in the bounded search. No new annotation or experiment is admitted.

The user has supplied **`xong`**. The prior operational handoff is resolved:
[Cycle7 verification](evidence/C27_RECOVERY_RESULT.md) accepts519 DEV and7096 TRAIN
recovered artifacts within the documented scope. All13 TRAIN cases failing the
CTC length condition are confirmed through the native loader; unchanged C27
training is technically ineligible and still lacks scientific pilot support.
No new training or human annotation. The research objective is **not achieved**.
The goal tool now confirms **active** following resumption. This is not a
completion claim; the original research objective remains unchanged.

Cycle8 [frame-retention diagnostic](evidence/FRAME_RETENTION_RESULT.md) rules out
retained-frame omission by the64-window cap in all7615 recovered inputs. All
decoded DEV frames are covered. The13 CTC failures are16/17-frame inputs with
no frame removal, not a support-filter defect. No sampling repair is admitted.

Cycle9 verifies an [existing Public DGS annotation resource](evidence/PUBLIC_DGS_RESOURCE_SCOPE.md)
for a possible within-parent type/subtype diagnostic. Posted use restrictions
need clarification for this project before corpus acquisition or model use.
This is a conditional evidence route, not PHOENIX relevance labels or a pilot.

Cycle10 [within-run score-drift analysis](evidence/SCORE_DRIFT_RESULT.md) is
PROGRESS: six fixed step111/666 fusion matrices replay exactly, and a second
analysis is byte-identical. In every seed/direction, rising competitor scores
can accompany improving paired margins. Raw-score drift is not a sufficient
rank-harm label; no negative-mining or calibration method is admitted. This
diagnostic needed neither MY DGS permission nor new semantic judgments.

Cycle11 [handshape-resource verification](evidence/HANDSHAPE_RESOURCE_SCOPE.md)
adds PHOENIX14T-HS to the evidence/prior-art map, but rejects using its weak
gloss-level labels as independent token-level video truth. A dataset-name match
does not resolve Q04/Q17 attribution. No data/model acquisition or new auxiliary
is admitted; MY DGS permission remains a resource-specific issue, not a global
research gate. The preceding permission-only turn was NO PROGRESS.

Cycle12 [admission-scope audit](evidence/ADMISSION_SCOPE_AUDIT.md) corrects an
overbroad self-imposed prerequisite: independent semantic contrasts are not
required for every diagnostic or pilot. Official-pair computational/statistical
tests are allowed within their claim scope and the guide's actual gates.
Semantic attribution still requires appropriate evidence. PH-first transfer is
one confirmation design, not a mandatory discovery order. No route is promoted;
this is a protocol correction, not an empirical result.

Cycle13 [pooling-prior transfer screen](evidence/POOLING_PRIOR_TRANSFER_SCREEN.md)
adds a bounded source qualification but is **NO PROGRESS toward mechanism
admission**. The examined pooling/channel-loss routes encounter existing
operators, prior art and closed local tests; no new bottleneck or separating
intervention was established. No training, human annotation or method admission.

Cycle14 [paired-TRAIN-anchor screen](evidence/PAIRED_ANCHOR_SCREEN.md) rejects
the generic relative-representation route after a direct RELIT prior collision
and absent local bottleneck evidence. Its algebra is not universally a fixed
candidate offset; that distinction does not imply novelty or efficacy. No feature
extraction is justified for this specification. No candidate is pilot-supported.

Cycle15 [duplicate-wrist TRAIN census](evidence/WRIST_CONSISTENCY_RESULT.md)
is empirical diagnostic PROGRESS:0/653774 eligible retained frames meet the
registered strong-disagreement criterion;6944/7096 clips meet coverage.
The frequent sustained-disagreement lead is NO_GO, with exact replay. This
does not prove anatomical correctness or absence of other pose errors. No new
method, input repair, training or semantic annotation is admitted.

Cycle16 [batch-competition screen](evidence/BATCH_COMPETITION_SCREEN.md) restores
the omitted C18 negative to the reconstructed blacklist. Current schedules
verify equal sample exposure but unequal optimizer updates; both selected score
matrices replay exactly. Reject generic larger/cached-batch rescue as a new
method, without claiming that C18 identifies negative-cardinality causality.
This is bounded screening/artifact progress, not new efficacy or pilot support.

Cycle17 [published comparison contracts](evidence/FRONTIER_CONTRACTS_20260923.md)
add11 versioned result rows and explicit resource/selection gaps to file04.
Published directional leaders differ, and reported H2S populations differ;
neither a synthetic best-of-methods row nor local-DEV/published-TEST subtraction
is valid evidence of a gain. CMCM numbers/final protocol certification remain
unverified. This is frontier-documentation progress, not a new method or pilot.

Cycle18 [CSL text-input census](evidence/CSL_TEXT_COLLISION_RESULT.md) finds
zero exact deployed-input collisions among797 DEV groups. TRAIN has52/6598
participating groups; uniform token selection adds no pairs in either split.
Native-array parity passes all7395 groups, and two executions are byte-identical.
The registered dominant-collapse lead is NO_GO; no semantic, performance or
method claim follows. No new annotation, training or TEST access.

Cycle19 [GCN BatchNorm screen](evidence/GCN_BATCHNORM_SCREEN.md) rejects current
training-batch-statistics contamination as a method lead: the selected control
keeps GCN BN in eval while learning its weights. Native-block CPU fixtures and
existing policy tests pass6/6. Historical trainer/masked-policy hashes differ
from current files; Cycle20 locates and verifies their saved source snapshots.
This is still not fresh historical execution verification. No BN repair pilot.

Cycle20 [temporal padding diagnostic](evidence/GCN_TEMPORAL_PADDING_RESULT.md)
finds an active computational dependency: all four fixed TRAIN body-GCN outputs
change at their last four valid frames when zero-pose padding is appended.
Relative L2 changes6.57–18.26%; interior differences≤1.34e-15; extension4/8
outputs agree exactly. Two CPUfloat64 executions are byte-identical. This
admits downstream attribution only, not a method/pilot or retrieval-harm claim.
Next separating test is propagation through native three-part pose/sign_conv,
not a normalization sweep. No labels, TEST, GPU or training.

Cycle21 [native pose propagation](evidence/POSE_PADDING_PROPAGATION_RESULT.md)
finds the padding difference survives all three pose parts and actual sign_conv
pooling:13/13 selected boundary windows change, with unchanged interior/invalid
controls on the fixed four TRAIN videos. Final v3 replay is byte-identical;
an auxiliary cross-version exact check fails at≤5.56e-17 and is disclosed.
Proceed only to final-score sensitivity, not a method pilot or repair sweep.
No retrieval metric, training, GPU, TEST or new annotation.

Cycle4 is **PROGRESS**: a bounded
six-route screen and four-perspective review are complete. Five routes are
rejected as proposed; inherited C27 stays OPEN, not pilot-supported. Adjacent
priors constrain pair cross-attention, candidate-set comparison and decoder
likelihood scoring. This is selection-stage research, not efficacy.
Cycle3 was **PROGRESS**: mandatory
proposal7 historical reading completed, remaining named baseline mechanisms
traced, vendor ancestry assumption corrected, and a repetitive proposed next
step removed. This is audit progress, not a new efficacy experiment.
Cycle 2 was **PROGRESS**: nine retained
DEV score matrices replayed, persistent errors measured, and a 38-question map
completed. Cycle 1 revalidated negatives and measured TRAIN gloss structure.
None of these cycles establishes method efficacy. Cycle7 inspected the terminal
recovery records once after `xong`, then verified static outputs. There is no
remaining recovery wait and no current-process-liveness claim.

Authority: attached user text and
[proposal9 guide](../proposal9/CODEX_SLRET_AUTONOMOUS_GOAL.md). Work uses
academic-research-suite inline evidence verification and adversarial review;
the explicit autonomous task supplies continuing authorization for read-only
research. No independent reviewers or full systematic review are claimed.

## Required first checkpoint

1. **What is reproducible?** Existing CiCo and SEDS code paths, configs and
   historical replay/continuation results provide usable starting controls.
   Nine retained GCN-R1 matrices reproduce their stored metrics and per-query
   ranks exactly; this is saved-score replay, not fresh model inference. All 12 original
   negative gate files in the proposal7 ledger still hash-match. C27's three
   synthetic CTC tests and the two initial audit fixtures pass. Recovery now has
   checked artifacts and four additional audit fixtures. End-to-end C27 remains
   unverified; its unchanged all-TRAIN recipe fails13 length conditions. Recovery
   is not proof of numerical parity with the deleted cache.
2. **Strongest fair baseline?** The strongest recorded practical PH incumbent
   is adapted SEDS GCN-R1 seed1337, selected step222, mean DEV R1 **78.709056**
   (T2V 77.456647 / V2T 79.961464). It is not a universal SOTA result or a matched
   control for every future run. C27 requires a same-seed/scope/exposure native
   continuation and a same-gloss order-free auxiliary. CiCo remains a valid
   lower-resource comparator with different inputs.
3. **Closed ideas?** ELSC, DIVE, PLEL, OCEM, SSSC, sampling/partial alignment,
   PMGR, RPCA and close variants, plus later exact-recipe failures. See
   [blacklist](03_NEGATIVE_RESULTS_BLACKLIST.md). Deferred, invalid and timed-out
   trials are distinguished from empirical negatives.
4. **Unresolved empirical failures?** Across three selected GCN seeds, 113 T2V
   and 98 V2T errors persist. Of these, 88 and 74 respectively fail in both
   branches in all seeds. No persistent error has a strictly outranking exact
   gloss-multiset/different-order confuser. This rejects that narrow explanation,
   not all sequence supervision. Neither branch failure proves absent visual
   information. C26-A does not invalidate all translation donors.
5. **Unverified claims?** C27 efficacy/novelty, full extraction repeatability,
   strongest protocol-compatible frontier, full current baseline ancestry,
   new multi-seed gain and second-dataset/locked-test confirmation. No such
   claim is made.
6. **Top five highest-information questions?** The scored
   [38-question map](06_OPEN_RESEARCH_SPACE.md) ranks Q01 (82), Q23 (69),
   Q17/Q22 (67), and Q24 (65) highest. Q17 needs missing semantic labels and Q24
   is conditional; priority scores are not pilot admission. Cycle3 confirms that
   generic Q01 positive controls and broad Q22 impossibility arguments were
   already completed. Do not repeat them; a new separating mechanism is needed.
7. **Cheapest next experiment?** None is admitted yet. The
   [six-route screen](09_CANDIDATE_SCREEN.md) did not produce a supported survivor.
   A next diagnostic needs a specific hypothesis, separating control and
   predefined GO/NO-GO criteria, with evidence appropriate to its claim and
   distinction from closed work. It may test a computational/statistical
   mechanism using official pairs without independent semantic labels. No new
   empirical test is currently specified well enough to run; a candidate pilot
   must additionally satisfy all six guide section41 gates.
   Recovered IDs/window construction and native mask checks are now completed
   in their recorded scopes. Repeating them does not admit C27 training.
8. **Why informative?** Persistent errors constrain simple existing-score
   mixtures, but do not identify their cause. A diagnostic must discriminate
   the competing explanations relevant to its stated hypothesis; it need not
   resolve representation, optimization and semantic ambiguity all at once.
   Do not repeat failed heads under a new name.
9. **Necessary baseline checks?** Same inputs/IDs/gallery, scorer orientation,
   masks, strict checkpoint load, disabled-head parity, trainable-gradient
   path, RNG/batch order and DEV-only selection. Skip fresh exact-paper recall
   parity campaigns; existing usable controls are the starting point.
10. **Long operation handoff?** The C27 recovery handoff is resolved and terminal
    artifacts checked. No new long operation is admitted or launched.

## Completed execution checkpoint — do not relaunch

- Run: `c27-ph-feature-recovery-001`.
- Recorded supervisor PID: `1166311`; terminal records say COMPLETED, exit0.
- Working directory: `/home/haipd/SLR`.
- Entry point: `research/slret_dataset_first/tools/run_c27_feature_recovery.py`;
  exact launch argv is `/home/haipd/miniconda3/bin/python -u
  /home/haipd/SLR/research/slret_dataset_first/tools/run_c27_feature_recovery.py --run`.
- Records: `artifacts/slret_dataset_first/jobs/c27-ph-feature-recovery-001/`
  containing `launch.json`, `run.log`, `status.json`, `summary.json`.
- Verified outputs: `features/seds-adapted-dev-c27-001` (519) and
  `features/seds-adapted-train-c27-001` (7096) beneath that job directory.
- Success: terminal completion with exit0, both exact counts and input contracts,
  checkpoint SHA256 `13ce77ad08808333e2d4f850c632ac6068c837108e15f1c3902dcbcf30842db9`,
  no TEST, within registered resource bounds, and safe resumption of the exact
  UniFormerV2 worker if it was paused. A `COMPLETED` string alone is insufficient
  if resume verification failed.
- Failure: terminal failed/timed-out stage, missing/corrupt required output or
  failed resume condition. A transient observation failure is not job failure.
- User `xong` received: targeted terminal check and scoped artifact audit complete.
  Supervisor elapsed6339.263507 seconds charged once; new-grant balance now
  11,537.649493 seconds. See the report for native-check scope and limitations.
  Do not repeat polling or infer a currently running process from these records.

## Requirement audit and next independent work

| Guide requirement | Current evidence | Completion |
|---|---|---|
| Attachment + full proposal9 guide | Read; instructions applied | Complete |
| Repository/history reconstruction | Census, gates, registry; all ten mandatory proposal7 history files and Q01–Q37 audit fully read | Named history requirement complete; wider original specs remain inherited/targeted |
| Third-party paths + fair comparison | Six named mechanisms traced from local code/primary papers; current source hashes and resource limits | Bounded comparison complete for screening; wider donors/ancestry/final frontier remain |
| Failure-layer map | 13 layers plus three-seed retained-score residual analysis | Initial descriptive map complete; causal attribution unresolved |
| ≥30 questions with all fields and priority rubric | 38 explicit records; fields, seven score components and sums validated | Written; deeper source verification remains |
| Top hypotheses + collision analysis | Explicit contracts, six-route collisions and four sequential reviewer perspectives | Bounded screen complete; surviving mechanism still absent |
| 5–10 materially distinct candidates | Six intervention routes screened; inherited/equivalent routes explicitly rejected | One admission screen complete; not six novel discoveries or successful method selection |
| Supported primary proposal + exact implementation plan | No surviving primary selected | Not complete; do not manufacture one |
| Controlled pilot through confirmation/SOTA gates | C27 has no efficacy result | Not complete |
| 15 named deliverables | Files00–09,13–14 now maintained | Files10–12 outstanding; no supported primary |

Next independent work must move beyond the rejected screen: formulate a
distinct falsifiable mechanism with evidence appropriate to the claim and a
separating test. A natural semantic contrast is one possible route, not a
universal prerequisite. Existing official pairs can support computational and
statistical diagnostics; semantic interpretation must remain qualified. No
empirical test is currently admitted; broad Q01/Q17/Q22 questions alone are not
an executable experiment. Do not repeat generic Q01 positive controls, Q22 scalar-score arguments,
source-bug certificates or asset-availability searches as new experiments.
Do not treat this partial audit or a prior final NO-GO report as completion of
the active goal. Do not let the inherited C27 name predetermine selection.
