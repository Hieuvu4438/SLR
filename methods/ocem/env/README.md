# Environments

`environment.lock.json` records the actual modern environment observed for
OCEM development. It is not evidence of CiCo legacy parity. The historical
CiCo environment remains documented in the pinned upstream source and must be
treated as a separate reference environment.

